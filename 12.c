

#include <stdio.h>
#include <stdlib.h>                                   

int  main(int agrc, char *argv[]){

	        int a= atoi(argv[1]);                                 int b= atoi(argv[2]);
                int c= atoi(argv[3]);                                 int d= atoi(argv[4]);                                 int e= atoi(argv[5]);
			                                   printf("%d" "%d" "%d" "%d" "%d\n", a, b, c,  d
												      , e );
							  if(printf("%d" "%d" "%d" "%d" "%d\n", a, b, c,  d, e )){
												                                                                    printf("%d" "%d" "%d" "%d" "%d\n", e, d, c, b, a );
																				                                                                                                                        }                                             
											                            return 0;

}
